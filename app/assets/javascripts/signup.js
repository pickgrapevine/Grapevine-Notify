// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
function searchLocation(eventHandler){

}
function addLocation(){

}
function removeLocation(){

}
function checkout(){

}

$(function(){
    $('#search_button').click(search_location);
    $('#add_location_button').click(addLocation);
    $('#remove_location_button').click(removeLocation);
    $('#checkout_button').click(checkout);
});
